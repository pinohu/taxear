# Fingerprint Registry

One row per shipped build. A planned build must differ from EVERY row below on at
least 4 of 6 dimensions (Grammar, Nav, Hero, Sequence shape, Close, Signature
move). If it fails, change the plan, not this log.

Registry created 2026-08-23. Empty at creation: the design pass in progress is the
first entry, so the gate has nothing to clear on this build.

| # | Date | Client / Site | Grammar | Nav treatment | Hero device | Sequence shape | Close pattern | Signature move |
|---|------|---------------|---------|---------------|-------------|----------------|---------------|----------------|
| 1 | 2026-08-23 | TaxEar (taxear.com) | Chaptered reference apparatus | Printed head on stock, ruled off; no fixed bar | Title page, type only, release line | 5 movements: title → outline → peak → entry → colophon | Colophon in running text; resolves, does not fade | Live figure citation — every number opens its own source and verification date |
