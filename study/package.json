{
  "name": "taxear-study",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "import": "node scripts/import_questions.mjs",
    "verify": "node scripts/verify_questions.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "astro": "^5.12.0"
  }
}
